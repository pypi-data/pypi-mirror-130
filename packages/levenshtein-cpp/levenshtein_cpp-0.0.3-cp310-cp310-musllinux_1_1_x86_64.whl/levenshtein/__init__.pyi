__version__: str


def distance(a: str, b: str) -> int:
    """Compute the levenshtein's distance between two strings"""


def ratio(a: str, b: str) -> float:
    """Compute the levenshtein's ratio between two strings"""
