"""
Autogenerated state module using `pop-create-idem <https://gitlab.com/saltstack/pop/pop-create-idem>`__

hub.exec.boto3.client.ec2.create_carrier_gateway
hub.exec.boto3.client.ec2.delete_carrier_gateway
hub.exec.boto3.client.ec2.describe_carrier_gateways
"""



import copy
from typing import *
import dict_tools.differ as differ

async def present(hub, ctx, name: Text, vpc_id: Text, tag_specifications: List = None)  -> Dict[str, Any]:
    r'''
    **Autogenerated function**
    
    Creates a carrier gateway. For more information about carrier gateways, see Carrier gateways in the Amazon Web
    Services Wavelength Developer Guide.

    Args:
        name(Text): A name, ID, or JMES search path to identify the resource.
        vpc_id(Text): The ID of the VPC to associate with the carrier gateway.
        tag_specifications(List, optional): The tags to associate with the carrier gateway. Defaults to None.

    Returns:
        Dict[str, Any]

    Examples:

        .. code-block:: sls

            resource_is_present:
              aws_auto.ec2.carrier_gateway.present:
                - name: value
                - vpc_id: value
    '''

    
    result = dict(comment="", changes= None, name=name, result=True)
    ret = await hub.exec.boto3.client.ec2.carrier_gateway.id(
        ctx,
        jmes_path=name
    )
    if ret["status"]:
        # name is now the first id that matched the JMES search path
        name = ret["ret"]
    

    
    before = await hub.exec.boto3.client.ec2.describe_carrier_gateways(name)

    if before:
        result["comment"] = f"'{name}' already exists"
    else:
        try:
            ret = await hub.exec.boto3.client.ec2.create_carrier_gateway(
                ctx,
                DryRun=ctx.test,
                ClientToken=name,
                **{"VpcId": vpc_id, "TagSpecifications": tag_specifications}
            )
            result["result"] = ret["status"]
            if not result["result"]:
                result["comment"] = ret["comment"]
                return result
            ret["comment"] = f"Created '{name}'"
        except hub.tool.boto3.exception.ClientError as e:
            result["comment"] = f"{e.__class__.__name__}: {e}"

    
    # TODO perform other modifications as needed here
    ...

    after = await hub.exec.boto3.client.ec2.describe_carrier_gateways(name)
    result["changes"] = differ.deep_diff(before, after)
    return result



async def absent(hub, ctx, name: Text, carrier_gateway_id: Text)  -> Dict[str, Any]:
    r'''
    **Autogenerated function**
    
    Deletes a carrier gateway.  If you do not delete the route that contains the carrier gateway as the Target, the
    route is a blackhole route. For information about how to delete a route, see DeleteRoute.

    Args:
        name(Text): A name, ID, or JMES search path to identify the resource.
        carrier_gateway_id(Text): The ID of the carrier gateway.

    Returns:
        Dict[str, Any]

    Examples:

        .. code-block:: sls

            resource_is_absent:
              aws_auto.ec2.carrier_gateway.absent:
                - name: value
                - carrier_gateway_id: value
    '''

    
    result = dict(comment="", changes= None, name=name, result=True)
    ret = await hub.exec.boto3.client.ec2.carrier_gateway.id(
        ctx,
        jmes_path=name
    )
    if ret["status"]:
        # name is now the first id that matched the JMES search path
        name = ret["ret"]
    

    

    before = await hub.exec.boto3.client.ec2.describe_carrier_gateways(name)

    if not before:
        result["comment"] = f"'{name}' already absent"
    else:
        try:
            ret = await hub.exec.boto3.client.ec2.delete_carrier_gateway(
                ctx,
                DryRun=ctx.test,
                
                **{"CarrierGatewayId": carrier_gateway_id}
            )
            result["result"] = ret["status"]
            if not result["result"]:
                result["comment"] = ret["comment"]
                return result
            result["comment"] = f"Deleted '{name}'"
        except hub.tool.boto3.exception.ClientError as e:
            result["comment"] = f"{e.__class__.__name__}: {e}"

    

    after = await hub.exec.boto3.client.ec2.describe_carrier_gateways(name)
    result["changes"] = differ.deep_diff(before, after)
    return result



async def describe(hub, ctx)  -> Dict[str, Dict[str, Any]]:
    r'''
    **Autogenerated function**

    Describe the resource in a way that can be recreated/managed with the corresponding "present" function

    
    Describes one or more of your carrier gateways.


    Returns:
        Dict[str, Any]

    Examples:

        .. code-block:: bash

            $ idem describe aws_auto.ec2.carrier_gateway
    '''

    
    result = {}

    ret = await hub.exec.boto3.client.ec2.describe_carrier_gateways(ctx)
    if not ret["status"]:
        hub.log.debug(f"Could not describe carrier_gateway {ret['comment']}")
        return result

    for carrier_gateway in ret["ret"]["TODO"]:
        new_carrier_gateway = [
                {"CarrierGatewayIds": carrier_gateway_ids, "Filters": filters, "MaxResults": max_results, "NextToken": next_token}
        ]
        result[carrier_gateway["carrier_gatewayId"]] = {"aws_auto.ec2.carrier_gateway.present": new_carrier_gateway}

        for i, data in enumerate(carrier_gateway.get("", ())):
            sub_carrier_gateway = copy.deepcopy(new_carrier_gateway)

            # TODO check for subresouruces
            sub_carrier_gateway.append({})
            sub_carrier_gateway.append({"name": "TODOs"})
            result[f"TODOs-{i}"] = {"aws_auto.ec2.carrier_gateway.present": sub_carrier_gateway}

    return result


