"""
Autogenerated state module using `pop-create-idem <https://gitlab.com/saltstack/pop/pop-create-idem>`__

hub.exec.boto3.client.greengrass.create_subscription_definition
hub.exec.boto3.client.greengrass.delete_subscription_definition
hub.exec.boto3.client.greengrass.get_subscription_definition
hub.exec.boto3.client.greengrass.list_subscription_definitions
hub.exec.boto3.client.greengrass.update_subscription_definition
"""



from typing import *
import dict_tools.differ as differ
async def present(hub, ctx, name: Text = None, amzn_client_token: Text = None, initial_version: Dict = None, tags: Dict = None)  -> Dict[str, Any]:
    r'''
    **Autogenerated function**
    
    Creates a subscription definition. You may provide the initial version of the subscription definition now or use
    ''CreateSubscriptionDefinitionVersion'' at a later time.

    Args:
        name(Text, optional): The name of the subscription definition. Defaults to None.
        amzn_client_token(Text, optional): A client token used to correlate requests and responses. Defaults to None.
        initial_version(Dict, optional): Information about the initial version of the subscription definition. Defaults to None.
        tags(Dict, optional): Tag(s) to add to the new resource. Defaults to None.

    Returns:
        Dict[str, Any]

    Examples:

        .. code-block:: sls

            resource_is_present:
              aws_auto.greengrass.subscription_definition.present:
                - 
    '''
    
    result = dict(comment="", changes= None, name=name, result=True)
    ret = await hub.exec.boto3.client.greengrass.subscription_definition.id(
        ctx,
        jmes_path=name
    )
    if ret["status"]:
        # name is now the first id that matched the JMES search path
        name = ret["ret"]
    

    
    before = await hub.exec.boto3.client.greengrass.get_subscription_definition(name)

    if before:
        result["comment"] = f"'{name}' already exists"
    else:
        try:
            ret = await hub.exec.boto3.client.greengrass.create_subscription_definition(
                ctx,
                
                
                **{"Name": name, "AmznClientToken": amzn_client_token, "InitialVersion": initial_version, "tags": tags}
            )
            result["result"] = ret["status"]
            if not result["result"]:
                result["comment"] = ret["comment"]
                return result
            ret["comment"] = f"Created '{name}'"
        except hub.tool.boto3.exception.ClientError as e:
            result["comment"] = f"{e.__class__.__name__}: {e}"

    
    # TODO perform other modifications as needed here
    ...

    after = await hub.exec.boto3.client.greengrass.get_subscription_definition(name)
    result["changes"] = differ.deep_diff(before, after)
    return result

async def absent(hub, ctx, name: Text, subscription_definition_id: Text)  -> Dict[str, Any]:
    r'''
    **Autogenerated function**
    
    Deletes a subscription definition.

    Args:
        name(Text): A name, ID, or JMES search path to identify the resource.
        subscription_definition_id(Text): The ID of the subscription definition.

    Returns:
        Dict[str, Any]

    Examples:

        .. code-block:: sls

            resource_is_absent:
              aws_auto.greengrass.subscription_definition.absent:
                - name: value
                - subscription_definition_id: value
    '''
    
    result = dict(comment="", changes= None, name=name, result=True)
    ret = await hub.exec.boto3.client.greengrass.subscription_definition.id(
        ctx,
        jmes_path=name
    )
    if ret["status"]:
        # name is now the first id that matched the JMES search path
        name = ret["ret"]
    

    

    before = await hub.exec.boto3.client.greengrass.get_subscription_definition(name)

    if not before:
        result["comment"] = f"'{name}' already absent"
    else:
        try:
            ret = await hub.exec.boto3.client.greengrass.delete_subscription_definition(
                ctx,
                
                
                **{"SubscriptionDefinitionId": subscription_definition_id}
            )
            result["result"] = ret["status"]
            if not result["result"]:
                result["comment"] = ret["comment"]
                return result
            result["comment"] = f"Deleted '{name}'"
        except hub.tool.boto3.exception.ClientError as e:
            result["comment"] = f"{e.__class__.__name__}: {e}"

    

    after = await hub.exec.boto3.client.greengrass.get_subscription_definition(name)
    result["changes"] = differ.deep_diff(before, after)
    return result

