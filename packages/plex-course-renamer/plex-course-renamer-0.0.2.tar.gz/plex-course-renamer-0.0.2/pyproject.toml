[tool.poetry]
name = "plex-course-renamer"
version = "0.0.2"
description = ""
authors = ["Eric Warren <ericwarren99@hotmail.com>"]

[tool.poetry.dependencies]
python = "^3.10"
click = "^8.0.3"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
rename = 'plex_course_renamer.rename:run'