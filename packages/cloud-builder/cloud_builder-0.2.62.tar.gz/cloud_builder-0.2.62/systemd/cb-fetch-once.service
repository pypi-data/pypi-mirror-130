[Unit]
Description=CB Project Fetch Once
After=syslog.target
Requires=network-online.target

[Service]
EnvironmentFile=-/etc/cloud_builder
Type=oneshot
ExecStart=/bin/bash -c 'sudo cb-fetch --project ${CB_PROJECT} --single-shot'

[Install]
WantedBy=multi-user.target
