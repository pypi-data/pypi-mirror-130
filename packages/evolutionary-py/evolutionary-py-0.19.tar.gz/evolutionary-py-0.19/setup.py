from setuptools import setup, find_packages

setup(name='evolutionary-py', author='Ashish Juneja',description="A genetic algorithm framework for search optimization problems",version='0.19', packages=find_packages(),
      install_requires=['numpy', 'wheel'])
