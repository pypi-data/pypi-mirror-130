{
  "definitions": {
    "Communities": {
      "title": "OARepo Communities fields v1.0.0",
      "type": "object",
      "properties": {
        "oarepo:primaryCommunity": {
          "type": "string",
          "description": "Primary community ID of the record"
        },
        "oarepo:secondaryCommunities": {
          "type": "array",
          "items": {
            "type": "string",
            "description": "List of secondary community IDs where the record is also published"
          }
        },
        "oarepo:ownedBy": {
          "description": "Record owner - user that created/submitted the repository record for the resource",
          "type": "integer",
          "default": "Current authenticated user's ID"
        }
      },
      "required": [
        "oarepo:ownedBy",
        "oarepo:primaryCommunity"
      ]
    }
  }
}
